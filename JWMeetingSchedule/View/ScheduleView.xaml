<UserControl x:Class="JWMeetingSchedule.View.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JWMeetingSchedule.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock x:Name="TxtWeek" HorizontalAlignment="Center" Margin="0,10,0,0" FontSize="20" TextWrapping="Wrap" VerticalAlignment="Top"/>
        <ComboBox x:Name="ComboBoxMixer" DataContext="PeopleList" ItemsSource="{Binding MixerPeople}" HorizontalAlignment="Left" Margin="75,100,0,0" VerticalAlignment="Top" Width="120"/>
    </Grid>
</UserControl>
